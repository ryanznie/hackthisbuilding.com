# Ryan PR review

Reviewed PR #1 at `2e7dc8f06e018012a2549db1311f1c972e1ffd51` against `c0a71fec5846bd66ee38570e8ac50aa46c58c3c1`, and stacked PR #3 at `74188dd28456e285ec770225920d5270580130f5` against PR #1. The reference requirements are the PRD on GitHub main at review time. Local uncommitted product revisions were kept separate from the reviewed commits.

Initial recommendation: fix the findings before merging. All findings below were subsequently fixed and both PRs were merged, as recorded at the end. Both CI runs are green, but their existing tests miss the reproduced cases below. No GitHub reviews or comments have been submitted.

| Priority | PR | Finding | Evidence and consequence |
|---|---|---|---|
| P1 | [#3](https://github.com/ryanznie/hackthisbuilding.com/pull/3) | [Target failures never accumulate reliably](https://github.com/ryanznie/hackthisbuilding.com/blob/74188dd28456e285ec770225920d5270580130f5/display_runner/runner.py#L136) | `send()` enqueues without acknowledgment, then resets failures before the HTTP result. A reproduction produced 12 failed target POSTs across 24 successful source polls without the promised shutdown. Count completed deliveries. |
| P2 | #3 | [Animated primitives incorrectly become static](https://github.com/ryanznie/hackthisbuilding.com/blob/74188dd28456e285ec770225920d5270580130f5/simulator/worker/index.ts#L210) | Rain, waves, sparkles and rocket flames use elapsed time even with layer motion `still`. Waves, rain and Red Sox presets receive sequence zero despite different rendered frames at 0 and 1875ms, freezing the actual output. |
| P2 | #3 | [Static frames are deduplicated before delivery](https://github.com/ryanznie/hackthisbuilding.com/blob/74188dd28456e285ec770225920d5270580130f5/display_runner/runner.py#L133) | After the first static POST fails, subsequent polls skip `send()`, suppressing retries and hiding the error. A fail-once target received one POST across 20 polls and no successful frame. |
| P2 | [#1](https://github.com/ryanznie/hackthisbuilding.com/pull/1) | [Coordinate regex rejects ordinary pixel-art prompts](https://github.com/ryanznie/hackthisbuilding.com/blob/2e7dc8f06e018012a2549db1311f1c972e1ffd51/simulator/worker/generation.ts#L23) | Actual `checkPrompt()` rejects “Show pixel art of a moon”, “Pixel art of a sunset”, and “A pixel art dragon”. Alternatives `on` and `set` match word suffixes. Require actual coordinate or command syntax. |
| P2 | #1 | [Validation-error serialization breaks rejection handling](https://github.com/ryanznie/hackthisbuilding.com/blob/2e7dc8f06e018012a2549db1311f1c972e1ffd51/validator/app.py#L114) | Pydantic 2.12.5 preserves a `ValueError` object in `ctx.error` for forbidden public text. `json.dumps()` raises instead of returning 422. The Worker retries the invalid payload as a transport error, then reports validation unavailable instead of regenerating. Reproduced with `<Heart>` and a title containing a URL. |
| P2 | #1 | [Caller timeout is shorter than validator retry budget](https://github.com/ryanznie/hackthisbuilding.com/blob/2e7dc8f06e018012a2549db1311f1c972e1ffd51/simulator/worker/validator.ts#L12) | The caller aborts at 25s while Python allows three 20s attempts plus backoff. One timeout then a 6s successful attempt takes 26.25s and is discarded, potentially spawning overlapping retries. This is a code-path timing finding, not a measured long-running simulation. |

Validation: PR #1 passes typecheck, all 38 TypeScript tests, production build, and four Python validator tests. PR #3 passes typecheck, all 39 TypeScript tests, production build, and four Python runner tests. Reproductions used local inputs and mocked network failures; the review did not send frames to an organizer instance.

PRD integration gaps: the original PR #3 has no runner heartbeat or target-delivery feedback to pause the scheduler, and therefore does not satisfy transport-failure recovery by merely stopping its process. The local working product has since moved to one-minute turns, house lights, Pong and data buttons; those are separate revisions, not defects attributed to these PRs. PR #3 must follow PR #1 if merged as currently stacked.

## Fixes and merge verification

All six findings were corrected with regression coverage before merging. PR #1 was merged at `b5e78ff1cccf86be0ce1bb7fd432124e27ce7357` (September 13, 2026, 23:45 UTC); PR #3 was merged at `997bb2b962b3f52e678b9b71fa961648530d858a` (23:50 UTC). GitHub reports both as merged. The reviewed fixes also refresh static frames once per second so the organizer’s two-second stored-clip fallback cannot replace an active static frame. The combined product release adds heartbeat-based scheduler recovery and the 30-second games/data/screen-saver requirements. Actual organizer-instance output remains unverified.
